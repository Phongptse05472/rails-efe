<p id="notice"><%= notice %></p>

<h1>Courses</h1>

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Image</th>
    <th>Description</th>
    <th>Is free</th>
    <th>Is save</th>
    <th>Is owner</th>
    <th>Rate</th>
    <th>Number enrollment</th>
    <th>Enrollment date</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @courses.each do |course| %>
    <tr>
      <td><%= course.name %></td>
      <td><%= course.image %></td>
      <td><%= course.description %></td>
      <td><%= course.is_free %></td>
      <td><%= course.is_save %></td>
      <td><%= course.is_owner %></td>
      <td><%= course.rate %></td>
      <td><%= course.number_enrollment %></td>
      <td><%= course.enrollment_date %></td>
      <td><%= link_to 'Show', course %></td>
      <td><%= link_to 'Edit', edit_course_path(course) %></td>
      <td><%= link_to 'Destroy', course, method: :delete, data: {confirm: 'Are you sure?'} %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<h2>Top Rate Courses</h2>
<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Image</th>
    <th>Description</th>
    <th>Is free</th>
    <th>Is save</th>
    <th>Is owner</th>
    <th>Rate</th>
    <th>Number enrollment</th>
    <th>Enrollment date</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @rate_course.each do |course| %>
    <tr>
      <td><%= course.name %></td>
      <td><%= course.image %></td>
      <td><%= course.description %></td>
      <td><%= course.is_free %></td>
      <td><%= course.is_save %></td>
      <td><%= course.is_owner %></td>
      <td><%= course.rate %></td>
      <td><%= course.number_enrollment %></td>
      <td><%= course.enrollment_date %></td>
      <td><%= link_to 'Show', course %></td>
      <td><%= link_to 'Edit', edit_course_path(course) %></td>
      <td><%= link_to 'Destroy', course, method: :delete, data: {confirm: 'Are you sure?'} %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<h2>Free Courses</h2>
<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Image</th>
    <th>Description</th>
    <th>Is free</th>
    <th>Is save</th>
    <th>Is owner</th>
    <th>Rate</th>
    <th>Number enrollment</th>
    <th>Enrollment date</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @free_course.each do |course| %>
    <tr>
      <td><%= course.name %></td>
      <td><%= course.image %></td>
      <td><%= course.description %></td>
      <td><%= course.is_free %></td>
      <td><%= course.is_save %></td>
      <td><%= course.is_owner %></td>
      <td><%= course.rate %></td>
      <td><%= course.number_enrollment %></td>
      <td><%= course.enrollment_date %></td>
      <td><%= link_to 'Show', course %></td>
      <td><%= link_to 'Edit', edit_course_path(course) %></td>
      <td><%= link_to 'Destroy', course, method: :delete, data: {confirm: 'Are you sure?'} %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<h2>Free Courses</h2>
<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Enrollment date</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @sidebar_course.each do |course| %>
    <tr>
      <td><a><%= course.name %></a></td>
      <td> <%= course.enrollment_date %></td>

    </tr>
  <% end %>
  </tbody>
</table>
<br>

<h2>Display course by Topic</h2>

<table>
  <thead>
  <tr>
    <th>Course_Id</th>
    <th>Course</th>
    <th>Course</th>
    <th>Topic</th>
    <th>Enrollment Date</th>

    <th colspan="3"></th>
  </tr>
  </thead>
  <tbody>
  <% @topic_course.topic_courses.each do |topic_course| %>
    <tr>
      <td><%= topic_course.course.id %></td>
      <td><%= topic_course.course.name %></td>
      <td><%= topic_course.topic.name %></td>
      <td><%= topic_course.course.enrollment_date %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<br>

<h2>Display Topic by Course</h2>

<table>
  <thead>
  <tr>
    <th>Topic_Id</th>
    <th>Topic</th>
    <th>Course</th>
    <th>Enrollment Date</th>
    <th colspan="3"></th>
  </tr>
  </thead>
  <tbody>
  <% @course_topic.each do |course_topic| %>
    <% course_topic.courses.each do |x| %>
      <tr>
        <td><%= course_topic.id %></td>
        <td><%= course_topic.name %></td>
        <td><%= x.name %></td>
        <td><%= x.enrollment_date %></td>
      </tr>
    <% end %>
  <% end %>
  </tbody>
</table>
<br>


<%= link_to 'New Course', new_course_path %>
